    // cria um controlador e injeta o $scope
    angular.module('app')
    .controller('homeCtrl', function($scope) {
        // cria uma mensagem para exibir na view
       // $scope.mensagem = 'Se você está vendo isso, então o AngularJS está funcionando!';
    });
